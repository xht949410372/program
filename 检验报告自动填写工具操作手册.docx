 检验报告自动填写工具操作手册

 1. 软件概述

 1.1 功能介绍

本工具可以根据Excel码单中的数据，自动生成检验报告，包含以下功能：

- 读取码单中的包号（件号、Roll No、Package No、卷号、编号等）和数量信息
- 支持自定义包号/件号和数量栏的识别名称
- 计算实际码长（在吊码长基础上随机增加0.1-0.5）
- 生成随机门幅（142-142.5，保留一位小数）
- 为每卷生成随机扣分（可自定义范围）
- 支持.xlsx文件格式（建议使用）
- 按5卷一组分组显示数据
- 为每个sheet生成单独的Excel报告文件
- 智能识别不同名称的包号列和数量列
- 支持通过配置文件自定义包号/件号和数量栏的识别名称
- 计算100码评分
- 自动统计缸号信息

 1.2 系统要求

- 操作系统：Windows 7/8/10/11
- 不需要安装Python环境
- 存储空间：至少100MB

 2. 安装与配置

 2.1 安装步骤

1. 从提供的压缩包中解压文件到任意文件夹
2. 解压后会得到以下文件：
   - `auto_fill_report.exe`：主程序可执行文件
   - `config.ini`：配置文件

 2.2 配置文件说明

配置文件 `config.ini` 用于自定义包号/件号和数量栏的识别名称，以及其他格式选项。

 2.2.1 包号/件号识别名称配置

```ini
[PackageColumns]
 包号/件号的识别名称，用逗号分隔
names = 包号,件号,Roll No,Package No,卷号,编号
 数量栏的识别名称，用逗号分隔
quantity_names = 数量,件数,码数
```

修改方法：

1. 用记事本或其他文本编辑器打开 `config.ini` 文件
2. 在 `names` 后面添加或删除包号/件号的识别名称，用逗号分隔
3. 在 `quantity_names` 后面添加或删除数量栏的识别名称，用逗号分隔
4. 保存文件

例如，如果您的码单中包号列名为"批次"，数量列名为"米数"，可以修改为：

```ini
[PackageColumns]
 包号/件号的识别名称，用逗号分隔
names = 包号,件号,Roll No,Package No,卷号,编号,批次
 数量栏的识别名称，用逗号分隔
quantity_names = 数量,件数,码数,米数
```

 2.2.2 格式选项配置

```ini
[FormatOptions]
 是否在单元格内容前添加前缀，true为启用，false为禁用
add_prefixes = false
 是否需要实际码长，true为需要，false为不需要
include_actual_length = true
 最小分数
min_score = 5
 最大分数
max_score = 14
 计分项目数量
score_items_count = 28
 总扣分和其他之间的空行数量
empty_lines_between_total_and_other = 1
```

修改方法：

1. 用记事本或其他文本编辑器打开 `config.ini` 文件
2. 根据需要修改各个选项的值
3. 保存文件

选项说明：

- `add_prefixes`：是否在吊码长、实际码长和门幅前添加前缀
- `include_actual_length`：是否显示实际码长
- `min_score`：生成的扣分范围的最小值
- `max_score`：生成的扣分范围的最大值
- `score_items_count`：报告中显示的计分项目数量
- `empty_lines_between_total_and_other`：总扣分和计分项目之间的空行数量

 3. 使用方法

 3.1 启动程序

双击 `auto_fill_report.exe` 文件启动程序。

 3.2 选择码单文件

1. 程序启动后，会弹出文件选择对话框
2. 在对话框中选择您的码单文件（建议使用.xlsx格式）
3. 点击"打开"按钮
4. 如果您选择的是.xls文件，会弹出提示建议您将其另存为.xlsx格式

 3.3 生成报告

1. 程序会自动处理数据并生成报告
2. 生成的报告文件会保存在程序所在目录
3. 每个sheet对应一个报告文件，文件名使用sheet名称

 4. 报告格式说明

 4.1 报告结构

生成的报告包含以下内容：

| 项目     | 说明                                 |
| ------ | ---------------------------------- |
| 卷号     | 使用码单中的包号                           |
| 吊码长    | 码单中的数量                             |
| 实际码长   | 吊码长 + 0.1-0.5                      |
| 门幅     | 142-142.5（随机）                      |
| 计分     | 随机生成的扣分，每卷4列                       |
| 总扣分    | 各卷的总扣分                             |
| 100码评分 | 根据公式计算：(总扣分\3600)/(吊码长/0.9144)/56 |
| 缸号统计   | 统计不同缸号的总件数和总数量（如果码单中有缸号列）          |

 4.2 布局说明

- 每5卷为一组，分组显示
- 吊码长、实际码长、门幅和总扣分这四行对应的列是四个单元格合并成一个
- 所有数值居中显示
- 总扣分和计分项目之间有配置的空行数量
- 100码评分显示在总扣分的下一行
- 如果码单中有缸号列，会在报告最后添加缸号统计信息

 5. 常见问题与解决方案

 5.1 包号未被识别

问题：程序无法识别码单中的包号列

解决方案：

1. 检查码单中的包号列名称
2. 打开 `config.ini` 文件
3. 在 `names` 后面添加包号列的名称
4. 保存文件并重新运行程序

 5.2 数量列未被识别

问题：程序无法识别码单中的数量列

解决方案：

1. 检查码单中的数量列名称
2. 打开 `config.ini` 文件
3. 在 `quantity_names` 后面添加数量列的名称
4. 保存文件并重新运行程序

 5.3 编码错误

问题：运行程序时出现编码错误

解决方案：

1. 确保 `config.ini` 文件使用UTF-8编码
2. 用记事本打开文件，点击"文件"→"另存为"，在"编码"下拉菜单中选择"UTF-8"
3. 保存文件并重新运行程序

 5.4 文件格式问题

问题：程序提示将.xls文件另存为.xlsx格式

解决方案：

1. 打开您的.xls文件
2. 点击"文件"→"另存为"
3. 在"保存类型"下拉菜单中选择"Excel工作簿 (\.xlsx)"
4. 保存文件并重新运行程序

 5.5 分数范围问题

问题：生成的扣分不在期望范围内

解决方案：

1. 打开 `config.ini` 文件
2. 修改 `min_score` 和 `max_score` 的值
3. 保存文件并重新运行程序

 5.6 计分项目数量问题

问题：报告中的计分项目数量不符合要求

解决方案：

1. 打开 `config.ini` 文件
2. 修改 `score_items_count` 的值
3. 保存文件并重新运行程序

 6. 注意事项

1. 码单中的包号必须是数字格式
2. 数量列必须包含有效的数值
3. 生成的报告文件会保存在程序所在目录
4. 每5卷会自动分组显示
5. 如果码单中有多个sheet，会为每个sheet生成单独的报告文件
6. 第一次运行程序时，会自动创建 `config.ini` 文件（如果不存在）
7. 复制到检验报告时，注意粘贴后选择纯数值
8. 建议使用.xlsx格式的码单文件
9. 100码评分的计算公式：(总扣分\3600)/(吊码长/0.9144)/56

 7. 技术支持

如果您在使用过程中遇到任何问题，请联系技术支持：

- 邮箱：<support@example.com>
- 电话：123-4567-8910



版本信息：

- 版本：1.0.0
- 发布日期：2026-04-18
- 最后更新：2026-04-18

