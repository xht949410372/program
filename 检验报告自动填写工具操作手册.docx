# 检验报告自动填写工具操作手册

## 1. 软件概述

### 1.1 功能介绍

本工具可以根据Excel码单中的数据，自动生成检验报告，包含以下功能：

- 读取码单中的包号（件号、Roll No、Package No、卷号、编号）和数量信息
- 计算实际码长（在吊码长基础上随机增加0.1-0.5）
- 生成随机门幅（142-142.5，保留一位小数）
- 为每卷生成随机扣分（可自定义范围，默认5-14分）
- 支持.xls和.xlsx文件格式
- 按5卷一组分组显示数据
- 为每个sheet生成单独的Excel报告文件
- 智能识别不同名称的包号列
- 支持通过配置文件自定义包号标识符

### 1.2 系统要求

- 操作系统：Windows 7/8/10/11
- 不需要安装Python环境
- 存储空间：至少100MB

## 2. 安装与配置

### 2.1 安装步骤

1. 从提供的压缩包中解压文件到任意文件夹
2. 解压后会得到以下文件：
   - `auto_fill_report.exe`：主程序可执行文件
   - `config.ini`：配置文件
   - `README.md`：说明文档

### 2.2 配置文件说明

配置文件 `config.ini` 用于自定义包号标识符，默认包含以下标识符：

```ini
[PackageColumns]
# 包号/件号的识别名称，用逗号分隔
names = 包号,件号,Roll No,Package No,卷号,编号
```

**修改方法**：
1. 用记事本或其他文本编辑器打开 `config.ini` 文件
2. 在 `names` 后面添加或删除包号标识符，用逗号分隔
3. 保存文件

例如，如果您的码单中包号列名为"批次"，可以修改为：

```ini
[PackageColumns]
# 包号/件号的识别名称，用逗号分隔
names = 包号,件号,Roll No,Package No,卷号,编号,批次
```

## 3. 使用方法

### 3.1 启动程序

双击 `auto_fill_report.exe` 文件启动程序。

### 3.2 选择码单文件

1. 程序启动后，会弹出文件选择对话框
2. 在对话框中选择您的码单文件（支持.xls和.xlsx格式）
3. 点击"打开"按钮

### 3.3 设置扣分范围

1. 选择码单文件后，会弹出扣分范围设置对话框
2. 输入最低扣分（默认5）
3. 输入最高扣分（默认14）
4. 点击"确定"按钮
5. 如果不想设置，直接点击"取消"按钮，将使用默认值

### 3.4 生成报告

1. 程序会自动处理数据并生成报告
2. 生成的报告文件会保存在程序所在目录
3. 每个sheet对应一个报告文件，文件名使用sheet名称

## 4. 报告格式说明

### 4.1 报告结构

生成的报告包含以下内容：

| 项目 | 说明 |
|------|------|
| 卷号 | 使用码单中的包号 |
| 吊码长 | 码单中的数量 |
| 实际码长 | 吊码长 + 0.1-0.5 |
| 门幅 | 142-142.5（随机） |
| 计分 | 随机生成的扣分，每卷4列 |
| 总扣分 | 各卷的总扣分 |

### 4.2 布局说明

- 每5卷为一组，分组显示
- 吊码长、实际码长、门幅和总扣分这四行对应的列是四个单元格合并成一个
- 所有数值居中显示

## 5. 常见问题与解决方案

### 5.1 包号未被识别

**问题**：程序无法识别码单中的包号列

**解决方案**：
1. 检查码单中的包号列名称
2. 打开 `config.ini` 文件
3. 在 `names` 后面添加包号列的名称
4. 保存文件并重新运行程序

### 5.2 编码错误

**问题**：运行程序时出现编码错误

**解决方案**：
1. 确保 `config.ini` 文件使用UTF-8编码
2. 用记事本打开文件，点击"文件"→"另存为"，在"编码"下拉菜单中选择"UTF-8"
3. 保存文件并重新运行程序

### 5.3 文件格式问题

**问题**：程序无法读取码单文件

**解决方案**：
1. 确保码单文件格式为.xls或.xlsx
2. 检查文件是否损坏
3. 尝试将文件另存为新的Excel文件

### 5.4 分数范围问题

**问题**：生成的扣分不在期望范围内

**解决方案**：
1. 运行程序时，在扣分范围设置对话框中输入正确的范围
2. 最低扣分应小于或等于最高扣分
3. 建议范围：5-14分

## 6. 注意事项

1. 码单中的包号必须是数字格式
2. 数量列必须包含有效的数值
3. 生成的报告文件会保存在程序所在目录
4. 每5卷会自动分组显示
5. 如果码单中有多个sheet，会为每个sheet生成单独的报告文件
6. 第一次运行程序时，会自动创建 `config.ini` 文件（如果不存在）
7. 复制到检验报告时，注意粘贴后选择纯数值

## 7. 技术支持

如果您在使用过程中遇到任何问题，请联系技术支持：

- 邮箱：support@example.com
- 电话：123-4567-8910

---

**版本信息**：
- 版本：1.0.0
- 发布日期：2026-03-31
- 最后更新：2026-03-31